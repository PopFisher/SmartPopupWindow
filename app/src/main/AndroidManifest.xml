<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="popup.popfisher.com.smartpopupwindow" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".PopupWindowMainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomPosPopupActivity" />
        <activity android:name=".TopBottomArrowPopupActivity" />
        <activity android:name=".Android7_0PopupActivity" />
    </application>

</manifest>